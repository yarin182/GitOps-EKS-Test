apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkerd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argocd.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://helm.linkerd.io/stable
    chart: linkerd-control-plane
    targetRevision: 1.16.11
    helm:
      values: |
        # Linkerd configuration with automatic certificate generation
        global:
          clusterDomain: cluster.local
        # Let Linkerd generate certificates automatically
        # Remove all manual certificate configuration to use defaults
        # Resource limits for components
        proxy:
          resources:
            cpu:
              request: 10m
              limit: 100m
            memory:
              request: 20Mi
              limit: 250Mi
  destination:
    server: https://kubernetes.default.svc
    namespace: linkerd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
